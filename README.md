# CodexTranslator

macOS utility app that translates selected text with `codex exec` and shows the source and translated text in a floating panel.

## Local run

```sh
swift run CodexTranslator
```

The app uses `Control + J` as a global shortcut. macOS Accessibility permission is required so the app can copy the current selection.
